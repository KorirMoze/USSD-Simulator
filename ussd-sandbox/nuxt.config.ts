// nuxt.config.ts
export default defineNuxtConfig({
  modules: [
    '@nuxtjs/tailwindcss'
  ],
  // Remove any existing PostCSS configuration if you have it
  // The module will handle all Tailwind setup automatically
})